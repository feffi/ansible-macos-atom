---
- hosts: 127.0.0.1
  connection: local
  vars:
    DEBUG: true
    macos_atom:

      # Settings to be placed in the atom user preferences
      preferences:

      # Packages to be installed via apm
      packagecontrol:

  roles:
    - { role: ansible-macos-atom }
